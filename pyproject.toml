[project]
name = "gptme-stats"
version = "0.1.0"
description = "Usage and community stats for gptme, collected daily"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10",
    "requests>=2.32",
]
